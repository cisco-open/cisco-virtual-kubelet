# Test 08 — auto-revert PASSED (image v37, 2026-04-28)
#
# Outcome captured at the v37 stop-point after schema discovery via
# NETCONF <get-schema> + translator update.

CR phase = Failed
familyStatus:
  access_list_extended:
    state: Drifted
    message: 1 op(s) applied but 1 still pending
  interface_ethernet:
    state: Drifted
    message: 1 op(s) applied but 1 still pending

events:
  Normal  AppliedSuccess    iosxeconfig/test-08-confirmed-commit-auto-revert  applied 1 family change(s) successfully
  Normal  DriftDetected     iosxeconfig/test-08-confirmed-commit-auto-revert  family "interface_ethernet" drifted: 1 op(s) applied but 1 still pending
  Normal  DriftDetected     iosxeconfig/test-08-confirmed-commit-auto-revert  family "access_list_extended" drifted: 1 op(s) applied but 1 still pending
  Warning ReconcileFailed   iosxeconfig/test-08-confirmed-commit-auto-revert  drift persisted after revert

# Device-side post-test verification (console SSH):
#
#   show running-config | include access-list extended TEST-08
#     → (empty)
#   show running-config interface GigabitEthernet0/0 | include access-group
#     → (empty)
#
# The TEST-08-MGMT-LOCKOUT ACL and the Gi0/0 access-group binding
# are BOTH absent from the device after the auto-revert window. The
# apply landed tentatively under <commit><confirmed/>; the controller
# could not ConfirmCommit (its own session was filtered by the new
# ACL); the device's confirmed-commit timer expired and reverted
# running-config to the pre-commit state.

# What this proves
#
# Wave-10 confirmed-commit + auto-revert is end-to-end-validated
# against a live Cat9300 / IOS-XE 17.18.2:
#
#   1. <commit><confirmed/><confirm-timeout>30</confirm-timeout></commit>
#      submitted by the engine when spec.transactional=true and
#      spec.confirmTimeoutSeconds=30 are set.
#   2. The candidate change applied tentatively to running-config
#      (the ACL + interface binding were on the device for ~30s).
#   3. The new ACL filtered the controller's source IP, so the
#      controller's runningVerify Fetch failed.
#   4. The engine declined ConfirmCommit and set the CR to
#      revert-and-retry mode (driftPolicy=revert).
#   5. The device's own timer fired after 30s and reverted
#      running-config to the pre-commit baseline.
#   6. After the revert window the engine's next Fetch shows the
#      change is gone (drift), it re-applies, gets the same result,
#      and finally declares phase=Failed with the "drift persisted
#      after revert" signature.
#
# The CR ending in Failed is the documented expected-outcome per
# release-blocker-tests/08-confirmed-commit-auto-revert/README.md
# §"Expected" — the device successfully self-protected against the
# management-plane-breaking change.
