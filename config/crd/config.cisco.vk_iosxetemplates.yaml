---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: iosxetemplates.config.cisco.vk
spec:
  group: config.cisco.vk
  names:
    kind: IOSXETemplate
    listKind: IOSXETemplateList
    plural: iosxetemplates
    shortNames:
    - iosxetpl
    singular: iosxetemplate
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.referencers
      name: Refs
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: IOSXETemplate is a reusable, parameterised configuration fragment.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: IOSXETemplateSpec declares a parameterised configuration
              fragment.
            properties:
              configuration:
                description: |-
                  Configuration is the templated netascode fragment. The expander
                  substitutes {{ .Name }} references using the Go text/template engine
                  restricted to parameter lookups — no function calls, no file access.
                x-kubernetes-preserve-unknown-fields: true
              parameters:
                description: Parameters is the contract the referring CR supplies
                  values for.
                items:
                  description: |-
                    TemplateParameter declares one input to an IOSXETemplate. Expansion
                    fails (rather than silently substituting an unparseable value) when
                    a required parameter is missing or a supplied value fails the type check.
                  properties:
                    default:
                      description: |-
                        Default is the fallback value when the referring CR omits this
                        parameter. Stored as a string; parsed according to Type at expansion.
                      type: string
                    description:
                      description: Description is surfaced in tooling (kubectl explain,
                        lint output).
                      type: string
                    name:
                      description: Name of the parameter, referenced from the template
                        body as {{ .Name }}.
                      minLength: 1
                      type: string
                    required:
                      default: false
                      description: |-
                        Required marks the parameter as mandatory; an IOSXEConfig that
                        references this template without supplying a Required parameter fails
                        validation.
                      type: boolean
                    type:
                      description: |-
                        Type is the required value shape. Validation is performed at expansion
                        time against the rendered value; invalid inputs fail the expansion
                        rather than reaching the device.
                      enum:
                      - string
                      - int
                      - bool
                      - ipv4
                      - ipv6
                      - cidr
                      type: string
                  required:
                  - name
                  - type
                  type: object
                type: array
            required:
            - configuration
            type: object
          status:
            description: IOSXETemplateStatus tracks how many IOSXEConfig CRs reference
              this template.
            properties:
              conditions:
                description: Conditions follows the standard Kubernetes conditions
                  shape.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the .metadata.generation the aggregator
                  last read.
                format: int64
                type: integer
              referencers:
                description: |-
                  Referencers counts IOSXEConfig CRs whose spec.templateRefs include
                  this template by name.
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
